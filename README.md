# Expense Tracker

Full-stack expense tracker built with Express + TypeScript (backend) and Vite + React + TypeScript (frontend).

## Local development

```bash
# Backend
cd backend && npm run dev

# Frontend (in another terminal)
cd frontend && npm run dev
```
